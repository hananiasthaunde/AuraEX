{
  "name": "auraex-gestao-mentorias-mcp",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Sistema AuraEX com login, API protegida por sessão/PAT e servidor MCP Streamable HTTP.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check mcp.mjs && node --check mcp-fallback.mjs && node --check lib/security.mjs && node --check lib/rate-limit.mjs && node --check lib/data-store.mjs && node --check public/app.js && node --check public/login.js && node --check public/excel-bridge.js && node --check scripts/test-mcp.mjs && node --check scripts/generate-pat.mjs && node --check scripts/reset-admin-password.mjs",
    "test:mcp": "node scripts/test-mcp.mjs",
    "token:create": "node scripts/generate-pat.mjs",
    "admin:password": "node scripts/reset-admin-password.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
